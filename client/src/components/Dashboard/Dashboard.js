import React, { useState, Fragment } from 'react';

const Dashboard = (props) => {
  console.log(props);
  return (
    <Fragment>
      <div>Dashboard</div>
    </Fragment>
  );
};

export default Dashboard;
